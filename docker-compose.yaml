version: "3"
services:
    api:
        build: .
        ports:
            - "5000:5000"
        volumes:
            - .:/code
        working_dir: /code
        environment: 
            API_MODE: devel
        command: [ "make", "run" ]